services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=mail_trace_db'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=user'
    ports:
      - '5432:5432'
